# Animated-Components

This repository contains a collection of premium animated React and Next.js components. 

## Components Hub

We have created a central **Animation Components Hub** in the [`Root`](./Root) directory. This hub serves as a unified interface to explore all the components, view their documentation, and browse their core codebase.

### Getting Started with the Hub

To launch the central hub:
1. Navigate to the `Root` folder:
   ```bash
   cd Root
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Run the development server:
   ```bash
   npm run dev
   ```
4. Open [http://localhost:3000](http://localhost:3000) in your browser.

## Individual Components

Each component is also a standalone Next.js/React application:
- [`get hamburgur menu`](./get%20hamburgur%20menu)
- [`horizontal-scroll of cards on scroll`](./horizontal-scroll%20of%20cards%20on%20scroll)
- [`MacBook-Showcase-Landing-Page--React-Frontend`](./MacBook-Showcase-Landing-Page--React-Frontend)
